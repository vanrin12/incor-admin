// @flow
// libs
import React from 'react';
import IMAGES from 'themes/images';

export const Loading = () => (
  <div className="icon-loading">
    <img src={IMAGES.iconLoading} alt="loading" />
  </div>
);

export default Loading;
